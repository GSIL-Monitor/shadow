package gate_test

import (
	"testing"
	//	"log"
	//	//
	//	"shadow/agent/gate"
)

// func Locate(appName, key string) (conn redis.Connection, err error)
func TestLocate(t *testing.T) {
	//	app := "CPC"
	//	key := "php_booking_user_0001"
	//	conn, err := gate.Locate(app, key)
	//	log.Println(conn, err)
}
