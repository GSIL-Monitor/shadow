datetime:   2015-01-01 00:00        # be precise to minute
protocol_version:   1.0
owner:
alert_receivers:
migration:
app_names:
    - TEST
shards:
    - # one shard is one object
      master: localhost:6379,1      # ip:port,weight
      slaves: []                    # empty content
